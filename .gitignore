node_modules/
.next/

# Datenbank
*.db
*.db-wal
*.db-shm

# Env-Dateien mit API Keys — nie auf GitHub!
.env
.env.local
.env.*.local

# Lokale Daten (Uploads, Thumbnails, Media)
backend/data/

# Electron Build-Output (DMG ist zu gross für git)
dist/
out/

# CLIP bundle — build artifacts, nicht in git
# Erstellen mit: bash backend/scripts/build-clip-bundle.sh
backend/clip-env/
backend/clip-models/

# macOS
.DS_Store
.vercel

# Bundled native binaries (downloaded separately, not committed to git)
bin/mac-arm64/ffmpeg
bin/mac-arm64/ffprobe
bin/mac-arm64/yt-dlp
